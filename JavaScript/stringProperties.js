const s = 'technology, computer, it, code';

// String Properties.
// Properties doesnt ve parenthesis.
// methods ve parenthesis.

const length = s.length;
console.log(length);

const upcase = s.toUpperCase();
console.log(upcase);

const lowcase = s.toLowerCase();
console.log(lowcase);

// const substr = s.substring(0,5);
const substr = s.substring(0,5).toUpperCase();
console.log(substr);

console.log(s.split(', ')); 

 
